Blog Archive
============

{% for post in site.data.medium_posts %}
  <a href="{{ post.url }}" target="_blank">{{ post.title }}</a> ({{ post.date | date: "%b %Y" }})<br />
{% endfor %}
{% for post in site.posts %}
  <a href="{{ post.url }}">{{ post.title }}</a> ({{ post.date | date: "%b %Y" }})<br>
{% endfor %}

